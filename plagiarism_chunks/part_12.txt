= [ derived['sleep_score'] / 10.0, derived['hydration_score'] / 10.0, (10.0 - stress_level) / 10.0, (10.0 - fatigue_level) / 10.0, ] derived['recovery_score'] = float(np.mean(recovery_metrics)) # Progressive Overload Delta Formula base_rates = {'Beginner': 0.03, 'Intermediate': 0.05, 'Advanced': 0.07} exp_mods = {'Beginner': 0.70, 'Intermediate': 1.00, 'Advanced': 1.15} base_rate = base_rates.get(derived['experience'], 0.05) exp_mod = exp_mods.get(derived['experience'], 1.0) # Blend consistency proxy with streak logic days_pw = max(1, derived.get('days_per_week', 4)) streak_factor = min(derived.get('streak_count', 0) / max(1, 2 * days_pw), 1.0) adherence = 0.60 * derived['consistency_score'] + 0.40 * streak_factor derived['progressive_overload_delta'] = round( max(0.0, min(0.10, base_rate * adherence * derived['recovery_score'] * exp_mod)), 5 ) return derived Listing 7.2: ML Feature Extraction and Biometric Calculation Explanation: This module prepares raw user data for the XGBoost inference engine by deriving complex secondary metrics. It programmatically models biological phenomena like recovery capacity and progressive overload safely before generating the final tensor. 7.5.3 REST API: ML Inference Wrapper (FastAPI) File: backend-python/server.py Purpose: Serves as the primary bridge between the frontend and the pre-trained XGBoost models, loading the runtime and safely passing the derived JSON matrices. @app.post("/generate-plan") async def generate_plan( profile: dict, x_auth_token: Optional[str] = Header(None, alias="x-auth-token"), ): """Generate full workout + nutrition plan from profile data using ML Runtimes""" try: # Load pre-trained models via Joblib workout_runtime = _ensure_workout_engine_ready() meal_runtime = _ensure_meal_engine_ready() # Extract and validate user credentials request_id = str(uuid.uuid4())[:8] user_id = _require_user_id_from_token(x_auth_token, request_id) result = {"success": True, "data": {}} # Generate personalized workout plan via XGBoost Inference try: weekly_plan = workout_runtime.generate_weekly_plan(profile) result["data"]["workout_plan"] = weekly_plan result["data"]["workout_days"] = sum(1 for d in weekly_plan if d.get("type") == "workout") result["data"]["rest_days"] = sum(1 for d in weekly_plan if d.get("type") == "rest") except Exception as e: result["data"]["workout_error"] = str(e) return result except Exception as e: raise HTTPException(status_code=500, detail="Inference failure") Listing 7.3: XGBoost Endpoint and Generation Request Explanation: This API endpoint acts as the orchestration layer for the Python backend. It securely identifies the authenticated user via tokens, initializes the pre-trained machine learning engines into memory, and constructs the personalized response JSON. 7.5.4 Cybersecurity: JWT Session Management (Node.js) File: backend-node/middleware/auth.js Purpose: Stateless authentication middleware that prevents unauthorized access to database operations by verifying cryptographic JWT signatures on protected routes. const jwt = require('jsonwebtoken'); const User = require('../models/User'); module.exports = async function(req, res, next) { const token = req.header('x-auth-token'); if (!token) { return res.status(401).json({ message: 'No token, authorization denied' }); } try { // Cryptographically verify the token against the private server secret const decoded = jwt.verify(token, process.env.JWT_SECRET); req.user = decoded.user || decoded; // Verify the user exists and is not administratively suspended const user = await User.findById(req.user.id).select('isSuspended'); if (!user) { return res.status(401).json({ message: 'User not found' }); } if (user.isSuspended) { return res.status(403).json({ message: 'Account is suspended.' }); } // Proceed to protected route controller next(); } catch (err) { res.status(401).json({ message: 'Token is not valid' }); } }; Listing 7.4: JSON Web Token Verification Middleware Explanation: This security middleware intercepts incoming HTTP requests and decrypts the provided JWT. It validates the cryptographic signature to block forged requests, ensuring that sensitive fitness and dietary data is only accessible to the authenticated owner. 7.5.5 Workout Plan Construction Logic (Python) File: backend-python/app/workout_engine.py Purpose: Drives the deterministic logic for constructing the daily routines, securely mapping the ML outputs against safety protocols, available equipment, and physiological constraints. def _build_weekly_plan(self, profile: dict, split: List[str], rest_day_positions: List[int]) -> List[Dict]: # Isolate user parameters focus = partial_splits[split_idx] goal = profile.get('goal', 'Muscle Gain') experience = profile.get('experience', 'Beginner') equipment = profile.get('equipment', ['Body Weight']) body_issues = profile.get('body_issues', []) # Filter available exercises based on equipment and biomechanical safety limits exercises = self._get_exercises_for_day( focus, goal, experience, equipment, body_issues, profile, day_seed=day_seed, global_used_names=global_used_names, ) # Ensure exercise uniqueness across the weekly plan to prevent staleness for ex in exercises: global_used_names.add(ex.get('name', '')) # Build the full session block warmup = self._get_warmup_for_focus(focus) intensity_metrics = self._calculate_day_intensity(exercises, experience, goal) full_session_exercises = self._enforce_unique_media_per_day(warmup + exercises) # Append to weekly schedule matrix weekly_plan.append({ 'day_of_week': day_idx, 'focus': focus, 'warmup': warmup, 'exercises': full_session_exercises, 'type': 'workout', 'intensity': round(intensity_metrics.get('intensity_score', 0.0), 2) }) Listing 7.5: Dynamic Exercise Selection and Schedule Assembly Explanation: This segment handles the concrete construction of a workout session. It applies constraint-based filtering (such as filtering out exercises requiring unavailable equipment or posing biomechanical risks to injuries) to assemble a safe, highly personalized daily routine from the master database. CHAPTER 8: SOFTWARE TESTING 8.1 Testing Strategy Software testing is a fundamental phase in the Software Development Life Cycle (SDLC) that involves the execution of a system or component to evaluate its properties, verify that it meets the specified requirements, and identify defects. A robust testing strategy ensures both verification (building the product correctly) and validation (building the correct product). For a highly complex, distributed system like Elevate—which relies on real-time computer vision, machine learning inference, and secure service-oriented modules—a formal, multi-tiered testing methodology was essential to guarantee system reliability, predictive accuracy, and security. The testing strategy for Elevate was structured around the V-Model of software testing, progressing from granular code-level checks to holistic system validation. This strategy encompasses the following theoretical methodologies and their project-specific implementations: Automated Testing Methodology and Planning: The automated testing strategy for Elevate was systematically planned to target the most critical, data-heavy, and deterministic parts of the application: the backend machine learning pipeline and the security authentication gateways. The planning phase for automated testing involved three key steps: Identifying Deterministic Boundaries: The planning began by isolating functions that process fixed inputs into predictable outputs without requiring physical hardware interaction (like webcams). The Python XGBoost feature derivation matrices and the Node.js JSON Web Token (JWT) verification logic were identified as prime candidates for automation. Mocking and Test Data Generation: To ensure tests were repeatable and safe, a comprehensive suite of "mock" JSON user profiles was designed. These mock payloads represented edge-case physiological constraints (e.g., extremely